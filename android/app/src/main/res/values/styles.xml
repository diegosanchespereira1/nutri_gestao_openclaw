<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--
        Android 15+ (API 35+) com targetSdkVersion >= 35 força edge-to-edge.
        Android 16 (API 36) reforça isso ainda mais — o app DEVE desenhar
        atrás das barras do sistema. O Capacitor WebView lida com os insets
        via JavaScript; o tema aqui precisa apenas não bloquear esse modo.
        android:windowOptOutEdgeToEdgeEnforcement = false significa que
        ACEITAMOS edge-to-edge (não estamos optando por sair do modo).
    -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <!-- Ícones escuros na barra de status (fundo claro do app) -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@color/splash_background</item>
    </style>


    <!-- Android 12+ Splash Screen API (androidx.core:core-splashscreen) -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
